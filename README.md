# 📡 Read Data From ESP8266 D1 R1 (Serial Communication)

โปรเจคนี้เป็นตัวอย่างการสื่อสารระหว่าง  
**ESP8266 D1 R1 (NodeMCU)** กับ **Python บน Windows** ผ่าน Serial (USB)

---

## 🧰 อุปกรณ์ที่ใช้

- NodeMCU ESP8266 D1 R1
- สาย USB (ต้องเป็นสาย Data)
- คอมพิวเตอร์ Windows
- Python 3.12+

---

## 🔌 1️⃣ โค้ดฝั่ง ESP8266 (Arduino)

อัปโหลดโค้ดนี้เข้า ESP8266 ผ่าน Arduino IDE

```cpp
void setup() {
  Serial.begin(9600);
}

void loop() {
  Serial.println("Hello From ESP8266 D1 R1");
  delay(1000);
}
```

📌 ความเร็ว Baudrate = **9600**

---

## 🐍 2️⃣ ติดตั้ง Python Library

เปิด Command Prompt แล้วพิมพ์:

```bash
pip install pyserial
```

---

## 🔎 3️⃣ ตรวจสอบ COM Port

1. เสียบสาย USB
2. เปิด **Device Manager**
3. ดูที่หัวข้อ **Ports (COM & LPT)**
4. จำหมายเลข COM เช่น `COM5`

---

## 💻 4️⃣ โค้ด Python สำหรับอ่านข้อมูล

แก้ `"COM7"` ให้ตรงกับ COM ของคุณ

```python
import serial

# เปลี่ยน COM ให้ตรงกับของคุณ
ser = serial.Serial("COM7", 9600, timeout=1)

print("Connected to ESP8266...")

while True:
    if ser.in_waiting > 0:
        data = ser.readline().decode("utf-8").strip()
        print("Received:", data)
```

---

## ▶ 5️⃣ วิธีรัน

```bash
python test.py
```

ผลลัพธ์ที่ควรได้:

```
Connected to ESP8266...
Received: Hello From ESP8266 D1 R1
Received: Hello From ESP8266 D1 R1
Received: Hello From ESP8266 D1 R1
...
```

---

## ⚠ ปัญหาที่พบบ่อย

### ❌ PermissionError: Access is denied
- ปิด Serial Monitor ใน Arduino IDE ก่อน
- ปิดโปรแกรมที่ใช้ COM Port อื่น ๆ

### ❌ FileNotFoundError
- ตรวจสอบว่าใช้ COM ถูกต้อง
- เช็ค Device Manager ใหม่

### ❌ ไม่ขึ้น COM เลย
- ลง Driver CH340 หรือ CP2102
- เปลี่ยนสาย USB (บางสายเป็นสายชาร์จอย่างเดียว)

---

## 📌 หมายเหตุ

- Baudrate ต้องตรงกันทั้งฝั่ง ESP และ Python
- เปิด Serial Port ได้ครั้งละ 1 โปรแกรมเท่านั้น
- เมื่อ Python เปิดพอร์ต ESP8266 จะ Reset 1 ครั้ง (ปกติ)

---

## 🚀 ต่อไปสามารถทำอะไรได้บ้าง

- ส่งคำสั่งจาก Python ไปควบคุม ESP
- อ่านค่า Sensor จาก ESP แล้วบันทึกลง Database
- ทำระบบ IoT ผ่าน WiFi

---

**Author:** Wachirawit Tanleng  
**Platform:** Windows + Python + ESP8266
